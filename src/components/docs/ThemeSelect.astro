---
/*
 * Starlight's theme picker, removed.
 *
 * The site and the docs are deliberately one light Press surface. The design
 * system still carries a full dark palette because the desktop app uses it —
 * it just isn't a supported choice on the web. Rendering nothing keeps the
 * header layout intact without shipping a control that does nothing.
 */
---
