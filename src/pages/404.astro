---
import MarketingLayout from '../layouts/MarketingLayout.astro';

export const prerender = true;
---

<!-- Not indexable and no canonical. GitHub Pages serves this with a 404 for a
     missing path, but /404 and /404.html also answer 200, and the canonical it
     used to carry pointed at /404/, which does not exist. -->
<MarketingLayout
  title="Page Not Found — kindling Writer"
  description="The page you're looking for doesn't exist or has been moved."
  canonicalPath={false}
  noindex
>
  <main class="page-padding page-main">
    <div class="container">
      <div class="error-code">404</div>
      <h1>Page not found</h1>
      <p>The page you're looking for doesn't exist or has been moved.</p>
      <a href="/" class="back-link">Back to kindling</a>
    </div>
  </main>
</MarketingLayout>

<style>
  /* Page-specific styles only. Base styles in /assets/site.css */

  main.page-main {
    display: flex;
    justify-content: center;
    text-align: center;
  }

  .container {
    max-width: 560px;
  }

  .error-code {
    font-family: var(--font-display);
    font-size: 120px; /* decorative numeral, deliberately off-scale */
    /* 600 is Fraunces's ceiling under Press's web weight contract (0.14.0); a
       700 here only ever rendered as 600 once the web fonts were instanced. */
    font-weight: 600;
    color: var(--color-border);
    line-height: 1;
    margin-bottom: 16px;
  }

  h1 {
    font-family: var(--font-display);
    font-size: var(--text-h2);
    font-weight: 600;
    margin-bottom: 12px;
  }

  p {
    font-size: var(--text-body-lg);
    color: var(--color-text-muted);
    margin-bottom: 32px;
  }

  .back-link {
    display: inline-block;
    font-family: var(--font-ui);
    font-size: var(--text-base);
    font-weight: 500;
    color: var(--color-on-accent);
    background: var(--color-accent);
    padding: 14px 28px;
    border-radius: var(--radius-m);
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .back-link:hover {
    opacity: 0.9;
  }
</style>

<script>
  if (typeof gtag === 'function') {
    gtag('event', 'page_not_found', {
      page_location: window.location.href,
      page_referrer: document.referrer
    });
  }
</script>
